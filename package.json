{
  "name": "rts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vitest run && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.13",
    "vite-plugin-singlefile": "^2.3.0",
    "vitest": "3.2.4"
  },
  "dependencies": {
    "peerjs": "1.5.2"
  }
}
